# Port, unter dem die App im Browser erreichbar ist
WEB_PORT=8099

# Beim ersten Start die Positionen aus der Excel als Startdaten anlegen
SEED_ON_START=true

# Automatische Snapshots (0 schaltet sie ab)
BACKUP_INTERVAL_HOURS=24
BACKUP_KEEP=14
